# Terraform Plan Tutorial

## Create a new Terraform Plan

Create a new Terraform plan and save it as `tfplan-input-var`.

### Community Edition
```bash
$ terraform plan -out "tfplan-input-var"
added content by masiuddin
